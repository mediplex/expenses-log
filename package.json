{
  "name": "expenses-log",
  "version": "0.0.1",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "server:start": "yarn --cwd packages/server node .",
    "mobile:serve": "yarn --cwd packages/mobile ionic serve"
  },
  "dependencies": {},
  "devDependencies": {
    "@ionic/cli": "^6.5.0",
    "cordova-res": "^0.11.0",
    "firebase": "^7.13.1",
    "firebase-tools": "^8.0.2",
    "native-run": "^1.0.0"
  }
}
